function formatDate (date) {
                                  return new Date(date).toLocaleDateString('fr-FR');
                            };


module.exports = {
                    formatDate,
                 }